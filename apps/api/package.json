{
  "name": "@confession/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=../../.env src/server.ts",
    "start": "node --env-file-if-exists=../../.env dist/server.js",
    "check": "pnpm --filter @confession/contracts build && pnpm --filter @confession/database build && tsc --project tsconfig.json --noEmit",
    "test": "pnpm --filter @confession/contracts build && pnpm --filter @confession/database build && vitest run",
    "build": "pnpm --filter @confession/contracts build && pnpm --filter @confession/database build && tsc --project tsconfig.build.json"
  },
  "dependencies": {
    "@confession/contracts": "workspace:*",
    "@confession/database": "workspace:*",
    "@fastify/cors": "^11.3.0",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.10.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
